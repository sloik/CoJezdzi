
import Foundation
import AppFramework


public enum AccessibilityConstants {
   
    public enum mapScene: String {
        case styleButton
        case legalLabel
    }
}

public struct HelperMethods {
    
    static func costam() {
        
        DispatchQueue.main.asyncAfter(deadline: .now() + 3) {
            
        }
    }
    
    
}
